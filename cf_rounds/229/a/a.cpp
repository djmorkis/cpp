#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL_DEF
    #include <debug> 
#else
    #define debug(x) 42
#endif


int main() 
{
    int n, m;
    scanf("%d %d", &n, &m);
    string s;
    map<int,set<int>> x;
    for (int i = 0; i < n; ++i) {
        cin >> s;
        for (int j = 0; j < m; ++j) {
            x[i].
        }
    }
}
